---
- name: Nginx | Verify Amazon Linux
  ansible.builtin.include_role:
    name: general
    tasks_from: assert_amazon_linux.yml
  tags: ["always", "nginx"]

- name: Nginx | Include container tasks
  ansible.builtin.import_tasks: 10_container.yml
  tags: ["nginx", "container"]
