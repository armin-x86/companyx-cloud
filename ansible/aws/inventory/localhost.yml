---
# Use `ansible-playbook ... --limit '!local'` or separate plays to skip when targeting only AWS.
all:
  children:
    local:
      hosts:
        localhost:
          ansible_connection: local
          ansible_python_interpreter: "{{ ansible_playbook_python }}"
